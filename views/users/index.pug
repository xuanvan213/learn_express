h1 Users list 

a(href="/users/create") Create user

h2 Search 
    form(action="/users/search", method="GET") 
        input(type="text", name="name", value=`${(keywork !== undefined ? keywork : '')}`)
        button search
ul 
    each user in users
        li
            span= "Name: "  + user.name + ", age: "  + user.age
            span &nbsp;
            a(href='/users/' + user.id) Chi tiet